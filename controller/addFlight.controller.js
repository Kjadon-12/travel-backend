import { addFlightService } from "../services/addFlight.service.js";

export const addFlight = (req, res) => {

  const { formData } = req.body;
  //   return res.json(req);
//   console.log("Request:", req);
  return;
  // const result = addFlightService(formData);
};
